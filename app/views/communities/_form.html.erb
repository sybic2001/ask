<%= simple_form_for community do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :url %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.submit :button, value: t('.submit'), class: 'btn btn-primary' %>
<% end %>
