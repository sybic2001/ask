var edited_competency = $("<%= j render 'user_competency_card', user_competency: @user_competency, private_profile: true %>");
// Add it to the DOM, at the end of the existing reviews. It's still hidden.
$('#user-competency-' + <%= @user_competency.id %>).replaceWith(edited_competency);
setTimeout(function(){
$('#myModal').modal('hide');
}, 200);

