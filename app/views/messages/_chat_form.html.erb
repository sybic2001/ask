<%= simple_form_for [meeting, message], remote: true do |f| %>
  <%= f.input :description, as: :text, input_html: { rows: 3 }, label: false %>
  <%= f.submit :button, value: "Envoyer", class: 'btn btn-success' %>
<% end %>
