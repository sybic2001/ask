<%= simple_form_for(user_competency, remote: true) do |f| %>
  <%= f.input :competency_id, collection: Competency.all %>
  <%= f.input :level %>
  <%= f.submit :button, class: "btn btn-primary" %>
<% end %>
